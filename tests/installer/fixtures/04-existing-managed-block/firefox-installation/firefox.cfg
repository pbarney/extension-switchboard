// Shared config with an older managed block.
const BEFORE_MANAGED_BLOCK = "KEEP-BEFORE";

// BEGIN EXTENSION SWITCHBOARD MANAGED LOADER
(() => {
    const TARGET_PROFILE = "C:\\OldProfile";
    const SCRIPT_PATH = "C:\\OldScript.uc.js";
    const OLD_MANAGED_CONTENT = "MUST-BE-REPLACED";
})();
// END EXTENSION SWITCHBOARD MANAGED LOADER

const AFTER_MANAGED_BLOCK = "KEEP-AFTER";
