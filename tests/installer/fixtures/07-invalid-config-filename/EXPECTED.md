# 07-invalid-config-filename

Invalid configured filename should fail during context resolution before installation changes.

Expected installer exit code: `1`.

Use the root-level `Run-TestScenario.ps1` harness to stage and validate this fixture.
