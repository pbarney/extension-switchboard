# 08-comment-and-line-ending-repair

Existing files lack the required first-line comment and final newline.

Expected installer exit code: `0`.

Use the root-level `Run-TestScenario.ps1` harness to stage and validate this fixture.
